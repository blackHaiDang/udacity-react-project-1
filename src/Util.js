import * as BooksAPI from './BooksAPI'

// export const updateShelf = (shelvesObject) =>
//   fetch(`${api}/books/${bookId}`, { headers })
//     .then(res => res.json())
//     .then(data => data.book)
